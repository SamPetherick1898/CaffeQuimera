<h1>Bebidas</h1>

 <div class="drinks-container">
    
     {{#each drinks}}
     <div class="col-3 edge-padding">
            <div class="product-edge">
                    <img src={{url}} class="card-img-top max-img" alt="...">
                    <div class="card-body">
                        <h4 class="card-title">{{name}}</h4>
                        <p class="card-text">{{description}}</p>
                        <span class="badge text-bg-warning">Precio: ${{price}}</span>

                    </div>
                    <a href="/drinks/{{_id}}" class="d-grid gap-2">
                        <button type="button" class="btn"> Agregar a la orden</button>
                    </a>
            </div>
        </div>
     {{/each}}
  </div>
